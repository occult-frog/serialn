[project]
name = "serialn"
version = "1.0.0"
description = ""
requires-python = ">=3.11"
dependencies = ["click>=8.1"]

[project.scripts]
hi = "serialn:hi"
show_recipes = "serialn:show_recipes"

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"